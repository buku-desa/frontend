import React from "react";

export default function App() {
  return (
    <div style={{ fontFamily: 'Inter, system-ui, -apple-system, "Segoe UI", Roboto' }}>
      <h1>Halo — proyek TypeScript (.tsx) siap!</h1>
      <p>Ini file `src/App.tsx` contoh.</p>
    </div>
  );
}
